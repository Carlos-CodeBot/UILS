import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.uis.uils',
  appName: 'UILS',
  webDir: 'dist/UILS',
  server: {
    androidScheme: 'https'
  }
};

export default config;
