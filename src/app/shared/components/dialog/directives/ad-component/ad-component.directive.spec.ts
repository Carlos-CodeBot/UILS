// import { AdDirective, ViewContainerRef } from './ad-component.directive';

describe('AdComponentDirective', () => {
  it('should create an instance', () => {
    // const directive = new AdDirective(new ViewContainerRef());
    expect(true).toBeTruthy();
  });
});
